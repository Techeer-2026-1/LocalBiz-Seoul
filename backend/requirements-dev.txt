# Phase 1 Harness — 개발 도구 (런타임 의존성과 분리)
# 설치: source venv/bin/activate && pip install -r requirements-dev.txt
#
# 런타임 의존성은 requirements.txt 참조.

# Linter + formatter (Phase 1 Hard-constraint)
ruff==0.15.9

# Type checker (Phase 1 Hard-constraint)
pyright==1.1.405

# Git hook manager
pre-commit==4.0.1

# 향후 추가 예정 (Phase 5+):
# pytest-cov  -- 커버리지 리포트
